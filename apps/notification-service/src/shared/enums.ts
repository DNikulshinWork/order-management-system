export enum NotificationChannel {
  email = 'email',
  sms = 'sms',
  push = 'push',
  in_app = 'in_app',
}

export enum NotificationStatus {
  pending = 'pending',
  sent = 'sent',
  delivered = 'delivered',
  failed = 'failed',
  read = 'read',
}
